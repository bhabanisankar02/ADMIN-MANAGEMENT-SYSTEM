import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'practice.project',
  appName: 'practice-project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
